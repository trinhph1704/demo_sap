_schema-version: "3.2"
ID: demosap
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: demosap-destination-content
  type: com.sap.application.content
  requires:
  - name: demosap-destination-service
    parameters:
      content-target: true
  - name: demosap-repo-host
    parameters:
      service-key:
        name: demosap-repo-host-key
  - name: demosap-uaa
    parameters:
      service-key:
        name: demosap-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: demosap_html_repo_host
          ServiceInstanceName: demosap-html5-srv
          ServiceKeyName: demosap-repo-host-key
          sap.cloud.service: demosap
        - Authentication: OAuth2UserTokenExchange
          Name: demosap_uaa
          ServiceInstanceName: demosap-xsuaa-srv
          ServiceKeyName: demosap-uaa-key
          sap.cloud.service: demosap
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: demosap-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: demosap-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - demosap.zip
      name: demosap
      target-path: resources/
- name: demosap
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: demosap-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: demosap-destination-service
    service-plan: lite
- name: demosap-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: demosap-xsuaa-srv
    service-plan: application
- name: demosap-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: demosap-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
